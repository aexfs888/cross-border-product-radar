# 新手操作指南

## 1. 能不能同时运行多个 Hermes 项目？

可以。每个项目使用独立目录、数据库、日志和定时任务即可。本项目固定在 `E:\跨境热销商品`，不会占用已有的 `E:\fb+mb` 项目目录。建议不要让两个项目写入同一个数据库或同一个报表目录。

## 2. 第一次使用

项目已经完成初始化。您只需要先双击 `⑤系统体检.cmd`。看到 `FAIL` 时不要反复点击采集，把体检内容交给 Hermes；`WARN` 通常表示 GitHub 尚未登录，不影响本地采集。

## 3. 手动更新一次

双击 `②立即采集并更新报表.cmd`。它会依次：

1. 读取获准的免费公开来源；
2. 去重并保留证据；
3. 重新计算研究热度与商业可复用性；
4. 把商品严格分到两个正式库或 30 天待复核区；
5. 生成两份独立 Excel；
6. 备份到 H 盘。

普通热度不可复用商品不会出现在 `不可复用商品研究.xlsx`。
人物、赛事、票务、应用、订阅和纯服务也不会进入两个商品库；公开趋势偶然很热，不等于它是可上架商品。

## 4. 看结果

双击 `③打开只读看板.cmd`。浏览器会打开 `http://127.0.0.1:8765`。看板只绑定本机，不能从互联网访问，也不能修改数据库。

Excel 在：

- `E:\跨境热销商品\报表\可复用商品.xlsx`
- `E:\跨境热销商品\报表\不可复用商品研究.xlsx`

如果商品数为 0，请看“来源状态”和“待复核”数量。0 表示没有商品通过门槛，不应手工强制转库。

## 5. 连接 GitHub 免费云端采集

双击 `⑥连接GitHub云端采集.cmd`，在 GitHub 官方登录页由您本人登录。脚本会：

- 创建公开代码仓库 `cross-border-product-radar`；
- 只上传代码、规则和工作流；
- 把加密收件公钥保存为公开变量；
- 把签名私钥保存为 GitHub Secret；
- 触发首次加密采集测试。

私钥、数据库、Excel、商品档案和明文采集结果受 `.gitignore` 保护，不会进入仓库。登录前可继续使用本地采集。

## 6. 自动运行

- GitHub：每两小时采集并上传一个加密包；
- 台湾时间每天 04:37：只汇总云端运行健康，不解密商品；
- 每周一 05:47：运行分库与评分回测；
- Hermes：每 30 分钟检查新加密包；有新包才验签、解密、去重、分库、更新 Excel 并备份。

GitHub 定时任务不是实时服务，平台繁忙时可能延迟；Hermes 会按水位点补同步，不重复入库。

## 7. 可复用与不可复用素材

- 可复用库只接受明确允许商业使用、许可证和署名要求可验证的图片或视频；
- 不可复用研究库只记录公开 URL、类型、权利状态和研究说明；
- 不去水印、不破解 DRM、不复制平台视频、不把 Common Crawl 快照当成授权；
- 人工发现很火商品时，可记录公开链接，但没有授权前不能转到可复用库。

## 8. 出错时怎么办

- 单一来源失败：系统最多重试两次；连续三轮失败会暂停 12 小时，其余来源继续；
- GitHub 未登录：运行 `⑥连接GitHub云端采集.cmd`；
- Excel 生成失败：先运行 `⑤系统体检.cmd`；数据库和采集证据不会因此丢失；
- H 盘不可用：先恢复 H 盘连接，再运行 `④立即备份.cmd`；
- 数据库提示不是 `ok`：停止采集，保留 E/H 文件，不要删除；交给 Hermes 修复。

## 9. 恢复

H 盘采用内容寻址的增量快照，不会因为 E 盘误删而同步删除历史证据。数据库保留 7 个每日、5 个每周、6 个每月版本。恢复密钥位于 `H:\跨境热销商品\密钥恢复包`，不得上传或分享。

## 10. 结果应该怎样理解

- “高热”是多个公开信号的综合分，不是销量；
- “可复用”是系统级初筛通过，不是法律保证；
- “未知”表示证据不足，不能靠想象补齐；
- “来源冲突”必须人工核对品牌、型号、规格或价格；
- 任何上架决定仍需人工核验供应商、样品、检测、税务、包装、责任主体、退货与目标国家法规。
