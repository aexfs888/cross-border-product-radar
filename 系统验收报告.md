# 跨境热销商品雷达：系统验收报告

验收日期：2026-09-01（Asia/Taipei）

项目根目录：`E:\跨境热销商品`

备份根目录：`H:\跨境热销商品`

## 一、结论

本地系统已完成实施并通过验收。规划、数据库、免费公开来源采集、严格分库、完整商品说明、加密签名、Excel、只读看板、Hermes 定时同步及 H 盘版本化备份均可用。

当前正式结果为：可复用商品 0 个、高热不可复用研究商品 0 个、待复核线索 42 个。零正式商品是严格证据闸门的真实结果，不是错误；系统不会为了填满报表降低标准。

GitHub 云端部署状态：`未连接`。已连续 3 次进入 GitHub 官方设备授权流程，但本机网络在最后凭证交换接口均返回 `EOF`；本地仓库、工作流、公钥变量和 Secret 写入脚本均已就绪。网络恢复后双击 `⑥连接GitHub云端采集.cmd` 即可继续。在此之前，本地采集、分析、报表、Hermes 任务和备份均可正常使用。

## 二、实施前强制保存闸门

| 检查项 | 结果 |
|---|---|
| 主规划文件可读取、中文正常 | 通过 |
| E/H《可复用与不可复用完全分库版》一致 | SHA-256 `0F317B5455EDD5F0680D007C9E9E2E4119C62583FB022FAB24110FE2FF62AC60` |
| E/H《研究全量保留版最终规划》一致 | SHA-256 `2E4C313A10CD550FD28EE9BDDC65C819A420CB15EB39552DC42B2FECF135EA3F` |
| 未覆盖用户已有文件 | 通过（实施前目录为空） |
| 项目与备份根目录未改变 | 通过 |

## 三、真实采集与数据验收

- 已接入 11 个目标国家配置。
- 一次真实云端模式采集产生 329 条事件，使用 21/250 个页面请求。
- 加密包 SHA-256：`d8e4f5a9590ab789de950163b4cf446ce58a2c7ccd49b67b8190c3e308c47b12`。
- 已完成签名验证、SHA-256 验证、解密、329 条事件数量校验、格式校验、去重、分库和加密包归档。
- 临时目录没有残留 `.jsonl` 明文采集结果。
- 已用一个“签名正确但事件数量错误”的加密包实测失败路径：同步命令返回失败状态 1、不推进水位，临时解密明文自动删除。
- 数据库快速完整性检查：`ok`。
- 当前数据库：42 个待复核线索、301 条官方安全/召回记录、0 个正式可复用商品、0 个高热不可复用研究商品。
- GDELT 在本机网络连续 3 轮不可达，已按规则暂停 12 小时；其他 7 个自动来源健康。来源恢复后会自动重试，不影响其他来源。

## 四、质量与分库验收

- 新商品默认不可复用。
- 只有身份、供应、物流、责任主体、风险、完整度、可信度和商业素材授权全部过关，才可进入可复用库。
- 不可复用商品只有当前或历史峰值热度达到 60 分才进入长期研究库。
- 普通热度不可复用线索不生成正式档案、不进入 Excel，最多观察 30 天，随后删除详情并仅留匿名墓碑。
- 正式两库只接收可上架实体商品；人物、赛事、票务、应用、订阅和纯服务即使很热也不能进入商品报表。
- 未知权利素材只保存公开链接和元数据。
- 自动测试 6/6 通过，类型检查通过。

## 五、报表与界面验收

- `可复用商品.xlsx`：10 个工作表，10 个工作表全部渲染验证，公式错误 0。
- `不可复用商品研究.xlsx`：10 个工作表，10 个工作表全部渲染验证，公式错误 0。
- 当前两份正式报表均为 0 个商品，与数据库严格分库结果一致。
- 中文看板首页返回 200，仅监听 `127.0.0.1:8765`。
- 非只读请求返回 405；内容安全策略已启用。

## 六、自动运行与备份验收

- GitHub Actions 已配置：每两小时加密采集、台湾时间每天 04:37 健康汇总、每周一 05:47 回测、每周安全扫描。
- GitHub Actions 依赖动作均锁定到完整提交哈希，上传步骤只允许 `.age`、清单和签名。
- Hermes 任务 ID：`99c97b80b9eb`；状态 `scheduled`；每 30 分钟无限循环。已在 Hermes 的最小 PATH 定时环境中修复工具定位并真实触发复验；最近一次状态 `ok`、错误为空、失败连续计数 0。
- H 盘数据库、证据、商品库、密钥恢复包和恢复记录已建立版本化备份。
- 数据库备份保留策略：7 个每日、5 个每周、6 个每月版本；商品和证据使用内容寻址增量清单，不传播 E 盘误删除。
- 已完成一次隔离恢复演练：从 H 盘恢复的数据库含 362 条事件，重放 329 条已签名加密事件后新增 0 条、总数仍为 362、完整性 `ok`；证明重复重放不会重复入库。演练临时明文和临时数据库已删除。

## 七、已知边界

- TikTok Creative Center、Amazon、Temu、AliExpress、Etsy、WIPO、欧盟 Safety Gate 和瑞士官方召回入口保持人工研究模式；系统不绕过登录、验证码、付费墙、DRM 或平台限制。
- 热度、搜索、新闻、广告和评价只是研究信号，不是销量或利润证明。
- “可复用”只是系统级初筛，正式上架前仍须人工核验供应商、样品、检测、授权、税务、包装、责任主体、退货和目标国家法规。

## 八、小白操作顺序

1. 双击 `⑤系统体检.cmd`。
2. 双击 `②立即采集并更新报表.cmd`。
3. 双击 `③打开只读看板.cmd` 查看结果。
4. GitHub 尚未连接时，双击 `⑥连接GitHub云端采集.cmd` 并只在 GitHub 官方页面确认登录。
5. 需要立即同步云端结果时，双击 `⑦同步GitHub并更新全部结果.cmd`。
